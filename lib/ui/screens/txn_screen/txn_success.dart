import 'package:flutter/material.dart';

class TxnSuccessScreen extends StatelessWidget {
  const TxnSuccessScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
